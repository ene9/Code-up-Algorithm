#include <stdio.h>

int main(void){
    
    char a;
    char b;
    
    scanf("%c %c", &a, &b); 
    printf("%c %c", b, a);
    
    return 0;
}


---------------------------------------------------------------------

이 문제는 2개의 문자를 입력받아 순서를 비꿔 출력하는 문제이다.
8번 코드 -> Visual Studio에서는  scanf_s("%c %c", &a, 64, &b, 64); 와 같이 
변수의 주소 뒤에 변수의 크기를 지정해 주어야 에러가 안뜬다.
