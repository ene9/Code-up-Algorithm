#include <stdio.h>

int main(void){
    
    int f, b;
    scanf("%d-%d", &f, &b);
    printf("%06d%07d", f, b);
    
    return 0;
}


----------------------------------

주민번호 앞 6자리와 뒷 7자리가 '-'로 구분되어 입력된다.
(입력값은 가상의 주민번호이다.)
ex)110011-0000000
'-'를 제외한 주민번호 13자리를 모두 붙여 출력한다.

7번 코드
정수형 변수 f에 000726라는 숫자가 입력되고, printf("%d", f);의 결과로는 726이 될 것이다.
그러므로 %d가 아닌 %06d로 서식지정자를 써서 printf로 출력해주면 입력한 값 그대로 000726이 출력될 것이다. 
