#include <stdio.h>

int main(void){
    
    char str[51];
    scanf("%s", &str, 51);
    printf("%s", str);
    
    return 0;
}


--------------------------------------------------------------------

문자열 입력
scanf("%s", str, sizeof(str));

문제에서 입력될 문자는 50자 이내라고 했다. 
문자열이 배열 형태로 변수에 저장될 때, 마지막에 NULL문자가 자동대로 포함되므로 공간을 하나 더해서 문자열 변수 str의 크기를 51로 지정했다.
