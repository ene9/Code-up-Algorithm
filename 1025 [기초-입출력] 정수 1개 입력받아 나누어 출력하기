#include <stdio.h>

int main(void) {
    
    int a, b, c, d, e;
    scanf("%1d%1d%1d%1d%1d", &a, &b, &c, &d, &e);

    printf("[%d]\n", a * 10000);
    printf("[%d]\n", b * 1000);
    printf("[%d]\n", c * 100);
    printf("[%d]\n", d * 10);
    printf("[%d]", e);

    return 0;
}



------------------------------------------------------

scanf("%d%d%d%d%d", &a, &b, &c, &d, &e);
처음에는 이렇게 썼는데 에러가 났다. -> 입력할 정수 간의 구분이 없어서 어떤 변수에 어떤 수를 저장할 지 명확하지 않아서 에러가 난 듯
scanf("%1d%1d%1d%1d%1d", &a, &b, &c, &d, &e); 
이렇게 하면 1개의 숫자로 각각 구분되어 저장된다. 

이제까지는 점(.)이다 띄어쓰기 등으로 구분했었는데 이와같이 붙여쓸 경우, 각 저장공간에 저장할 수의 갯수를 지정해야 한다. 
