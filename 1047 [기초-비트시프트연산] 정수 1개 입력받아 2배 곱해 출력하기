#include <stdio.h>

int main(void){
    
    long long int a;
    scanf("%lld", &a);
    printf("%lld\n", a<<1);
    
    return 0;
}


------------------------------------

연산은 논리 연산(AND, OR, NOT 등)과 비트 연산(shift left, shift right 등)이 있다. 
비트 연산 중 '시프트 연산'
shift left(<< n) :  * (2의 n승)
shift right(>> n) :  / (2의 n승)

