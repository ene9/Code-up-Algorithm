#include <stdio.h>

int main(void){
    
    int a, b;
    scanf("%d %d", &a, &b);
    printf("%d\n", ((a > b) ? 1 : 0));
    
    return 0;
}


----------------------------------------------


이 문제는 첫 번째 입력된 정수(a)가 두 번째 입력된 정수(b)보다 크면 1을 출력하고
그렇지 않으면(a <= b) 0을 출력하는 문제이다.

7번 코드  ->  조건 (a>b)가 참일 경우 1을 출력, 그렇지 않으면 0을 출력한다.

