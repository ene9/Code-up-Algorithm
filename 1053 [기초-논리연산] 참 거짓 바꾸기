#include <stdio.h>

int main(void){
    
    int a;
    scanf("%d", &a);
    printf("%d", !a);
    
    return 0;
}


----------------------------


연산자의 종류로는 수식연산, 논리연산, 비트연산이 있다.
논리 연산은 대표적으로 AND, OR, NOT이 있다.
각 논리 연산의 기호는 &&, ||, !이다.

여기서 주의할 것은 논리 연산의 NOT(!)과 비트 연산의 NOT(^)이다.
