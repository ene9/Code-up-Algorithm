#include <stdio.h>

int main(void){
    
    int a, b;
    scanf("%d %d", &a, &b);
    
    printf("%d\n", !(a^b));
    
    return 0;
}


-----------------------------------------

두 값이 같을 때, 참(1) -> Exclusive-Nor(XNOR)
NOT + XOR

두 값이 같을 때, 참(1)을 출력하는 것은 이 논리 연산이 아닌 비교 연산자를 사용해서도 쉽게 해결할 수 있다.

8번 코드를 printf("%d\n", (a==b)); 이렇게 써도 결과는 같다.
