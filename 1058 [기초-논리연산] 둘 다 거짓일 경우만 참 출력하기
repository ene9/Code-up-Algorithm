#include <stdio.h>

int main(void){
    
    int a, b;
    scanf("%d %d", &a, &b);
    printf("%d\n", !(a||b));
    return 0;
}


---------------------------------------

두 값이 모두 거짓(0)일 때만 참(1)을 출력
논리 연산 OR의 반대의 결과를 나타내는 NOR 연산이다.
