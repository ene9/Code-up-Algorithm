#include <stdio.h>

int main(void){
    
    int a;
    scanf("%d", &a);
    printf("%d\n", ~a);
    return 0;
}


---------------------------------


연산자의 종류로는 수식 연산(+,-,*,/ 등), 논리 연산(AND, OR, NOT, XOR 등), 비트 연산(AND, OR, NOR, XOR 등)이 있다.
논리 연산과 비트 연산의 연산 과정은 같지만, 피연산자와 기호는 다르다.
논리 연산 AND(&&), OR(||), NOT(!), XOR(^)
비트 연산 AND(&), OR(|), NOT(~), XOR(^)
비트 연산은 해당 정수를 2진수 비트로 변환한 후, 해당 연산을 수행한다.
