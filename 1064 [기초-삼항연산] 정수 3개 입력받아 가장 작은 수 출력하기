#include <stdio.h>
int main(void){
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    printf("%d\n", ((a<b)&&(b<c))?a:((b<c)?b:c));
    return 0;
}


----------------------------------------------------

해당 문제는 삼항 연산자를 이용하여 세 가지 정수 중 가장 작은 정수를 출력하는 문제이다.


                     ((a<b)&&(b<c))?a:((b<c)?b:c)
     
조건 (a<b)&&(b<c)이 참(a가 제일 작음)일 경우 a가 출력되고, 
거짓일 경우(a>b 또는 b>c) (b<c)?b:c 이 연산이 수행된다.
                          -> b가 c보다 작을 경우 b가, 그렇지 않을 경우 c가 출력된다.
                         
 이 문제는 3항 연산자의 수행 순서를 알기 위함이다.                    
