#include <stdio.h>

int main(void){
    
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    if(a%2==0){
        printf("%d ", a);
    }
    if(b%2==0){
        printf("%d ", b);
    }
    if(c%2==0){
        printf("%d", c);
    }
    return 0;
}


----------------------------------------------

Q. 세 정수 a, b, c가 입력되었을 때, 짝수만 출력해보자.

여기서 if문 다음에 if else를 쓰면 안되는 이유 
: if else는 그 전의 조건문이 거짓일 때 수행되는 문장이다.

 if (a%2==0){
        printf("%d ", a);
    }
 if else (b%2==0){        // (a%2 != 0) && (b%2 == 0)
        printf("%d ", b);
    }
 if else (c%2==0){        // (a%2 != 0) && (b%2 != 0) && (c%2 == 0)
        printf("%d", c);
    }
    
 1 2 4가 입력 될 경우, 4는 출력이 안되고 2만 출력이 된다.  (a = 1, b = 2, c = 4)
 왜냐하면 실행 순서가 처음 if문 조건 (a%2==0)을 검사하고 거짓이므로 다음 조건 (b%2==0)을 검사한다.
 2는 짝수이므로 2를 출력하고 if문을 벗어난다. 
 이렇게 되면 마지막 조건인 (c%2 ==0)을 검사하지도 못한 채로 if문을 벗어나므로 원하는 출력을 얻을 수 없다.
