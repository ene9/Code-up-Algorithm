#include <stdio.h>

int main(void){
    
    int n;
replay:
    scanf("%d", &n);
    if(n!=0){  //  입력된 정수가 0이 아닐 때, 그 정수를 출력하고 replay 레이블로 이동
        printf("%d\n", n);
        goto replay;        // replay라고 적혀있는 레이블로 이동(실행흐름제어)
    }
    return 0;
}



----------------------------------------

Q. 0이 아니면 입력된 정수를 출력하고, 0이 입력되면 출력을 중단해보자.


goto 이름:
이 명령은 이름: 이 작성된 곳으로 프로그램의 실행 흐름을 바꾸어 준다.

"이름:" 과 같이 콜론(:)이 붙어있는 부분을 이름표(label, 레이블)라고 한다.
레이블은 특별한 선언 없이 사용할 수 있으며 언더바(_)나 영문자로 시작하면 된다.
레이블은 한 단어처럼 공백없이 모두 붙여 써야 한다.
